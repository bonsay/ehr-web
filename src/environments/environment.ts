export const environment = {
  production: false,
  // During `ng serve`, requests to /api are proxied to the EHR API
  // (see proxy.conf.json). A relative base keeps the app origin-agnostic.
  apiUrl: '/api'
};
